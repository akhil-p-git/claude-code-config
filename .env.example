# Claude Code Environment Variables
# Copy this to ~/.zshrc or ~/.bashrc and fill in your actual values

# Required for Claude Code
export ANTHROPIC_API_KEY='your-anthropic-api-key-here'

# Required for GitHub MCP server (create at https://github.com/settings/tokens)
# Needs scopes: repo, read:org, read:user
export GITHUB_TOKEN='your-github-personal-access-token-here'

# Optional: for taskmaster MCP server (uses ANTHROPIC_API_KEY by default)
# export TASKMASTER_API_KEY='your-api-key-here'
